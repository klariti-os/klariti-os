// Server-side exports (use "@klariti/auth/server" for these)
export { auth, toNodeHandler, type Session } from "./auth";

// Client-side exports (use "@klariti/auth/client" for these)
export { authClient, signIn, signUp, signOut } from "./client";
